#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    cin >> N;

    vector<int> arr(N);
    for(auto& u : arr) cin >> u;
    
    int i = 0;
    int goods = 0;
    while(goods != N) {

    }

    return 0;
}