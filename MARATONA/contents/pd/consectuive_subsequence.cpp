/*
memo[0] = -1
memo[x+1] = l if (l > memo[x] && l < memo[x+1])
*/

#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    cin >> N;

    int memo[N];
    memset(memo, , sizeof())
}